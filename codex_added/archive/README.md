# Archive

This folder holds generated or superseded artifacts that are not needed for the current top-three inference runs.

- `results/`: old sweep, vote, finalized, taxonomy, and reward-debug outputs.
- `models/`: non-current smoke and GRPO adapters.
- `data/generated_training_data/`: derived SFT and external training datasets.

The active run dependencies remain outside the archive:

- `codex_added/models/qwen3_answer_format_lora`
- `codex_added/scripts/`
- `codex_added/math_comp/`
- `codex_added/configs/baseline.yaml`
