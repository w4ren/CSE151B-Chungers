# Added by Codex: local project dependencies; not part of the original starter repository.

# Core evaluation and prompting workflow.
accelerate==1.13.0
antlr4-python3-runtime==4.11.1
peft==0.15.2
numpy
PyYAML
sympy
tqdm
transformers==4.57.3
trl==0.19.1

# DataHub already provides a CUDA-enabled torch build for Transformers-only
# workflows. vLLM pins its own Torch/CUDA stack, so install this file for vLLM
# experiments with `uv pip install --torch-backend=auto -r codex_added/requirements.txt`.

# Optional later-stage packages:
bitsandbytes>=0.48.1
vllm==0.21.0
# ipykernel
# jupyter
